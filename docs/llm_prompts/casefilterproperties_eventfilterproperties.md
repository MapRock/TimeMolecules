Prompt: How do you construct `CaseFilterProperties` and `EventFilterProperties` JSON in TimeSolution, including how to find valid property names and determine whether each value should be alpha or numeric?

Abstract: `CaseFilterProperties` and `EventFilterProperties` in TimeSolution are JSON key-value objects used to restrict analysis to cases or events with specific parsed properties. The correct workflow is to inspect parsed-property sources such as `CasePropertiesParsed`, `EventPropertiesParsed`, `vwCasePropertiesParsed`, and `vwEventPropertiesParsed` to identify valid `PropertyName` values and determine whether each property is stored in `PropertyValueAlpha` or `PropertyValueNumeric`. That distinction controls how the JSON must be formed: numeric properties use numeric literals, while alpha properties use quoted strings. A reliable method is to inspect the available properties, confirm the exact name and type, build the JSON with the correct key-value pairs, and then pass it into the relevant TimeSolution procedures or functions. This prevents malformed filters and reduces the chance of empty or incorrect results.

Primary location of source material to analyze (for more information): https://github.com/MapRock/TimeMolecules/blob/main/tutorials/building_query_parameters
