You are an expert data architect performing semantic schema mapping across heterogeneous business systems (poker events, logistics, retail liquor sales, stock market data, EHR/medical records, etc.).

Here is the complete list of columns with their metadata:

{all_columns_str}

TASK:
- Consider every unique unordered pair of columns (SourceColumnID1 < SourceColumnID2). 
- Score semantic similarity from 0.0 (completely unrelated) to 1.0 (virtually the same business concept).
- Only include pairs that are "plausibly similar" (score >= 0.65) — e.g., columns that could be joined, mapped, or represent the same entity/metric across sources.

OUTPUT RULES (strict):
- Return ONLY valid CSV. No explanations, no markdown, no extra text, no duplicate pairs (i.e., if you include 1,3 you cannot include 3,1).
- Exact header row: SourceColumnID1,SourceColumnID2,SimilarityScore,Reason
- SimilarityScore must be a float with exactly two decimal places.
- Reason must be a short English phrase (max 80 characters) explaining the similarity.
- Sort by SimilarityScore DESC.

Example rows:
15,25,0.82,Unique participant/customer identifiers
1,3,0.91,Both represent bet/amount values in transactional events

Begin directly with the CSV header row.
