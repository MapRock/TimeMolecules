You are a precise semantic comparison engine specialized in healthcare, finance, and operational event data.

I will provide CSV data with these exact columns:
CaseID,NaturalKey,StartDateTime,EndDateTime,CaseTypeID,CaseTypeDescription,LinkedCaseID,LinkedCaseNaturalKey,LinkedCasePropertyName,LinkedCaseStartDate,LinkedCaseEndDate,LinkedCaseTypeID,LinkedCaseTypeDescription,CasesOverlap

For each row, compare the semantic meaning of **CaseTypeDescription** (primary case) and **LinkedCaseTypeDescription** (linked case).

Add **two new columns** at the very end:
- SemanticSimilar → exactly "Yes" or "No"
- Confidence → one of: High, Medium, Low

Decision Rules:
- "Yes" if the two descriptions belong to the same broad domain and represent closely related business processes (e.g., Emergency Room Case Management with ER Lab workflow or ER MRI workflow).
- "No" if they belong to unrelated domains (e.g., stock market performance data with any medical/ER process).
- Use CasesOverlap as supporting context but not as the sole deciding factor.

Output Instructions:
- Return ONLY the full CSV with all original columns plus the two new columns: SemanticSimilar,Confidence
- Do not add any explanations, summaries, markdown, or extra text before or after the CSV.
- Output must be valid CSV format. Enclose any field containing commas or quotes in double quotes.
- Keep the original column order and add the new columns at the end.

Here is the CSV data:

[Paste your full CSV content here]

Process the data and return the updated CSV with SemanticSimilar and Confidence columns.
