You are an expert data architect performing semantic schema mapping across heterogeneous business systems (poker events, logistics, retail liquor sales, stock market data, EHR/medical records, etc.).

Here is the complete list of columns with their metadata:

{all_columns_str}

TASK:
- Consider every unique unordered pair of columns (SourceColumnID1 < SourceColumnID2).
- Score semantic similarity from 0.0 (completely unrelated) to 1.0 (virtually the same business concept). Most highly weighted columns in order: ColumnIRI (if not NULL), ColumnDescription, ColumnName, SourceDescription.
- Only include pairs that are "plausibly similar" for example: 

(62, VisitID,Unique identifier for the emergency department visit or patient encounter) and (70,ParentVisitNatKey, Natural/business key linking the lab order to a parent hospital visit or encounter)

Example of not related is "F" for "Ford" (car company) and "F" for female.

OUTPUT RULES (strict):
- Return ONLY valid CSV. No explanations, no markdown, no extra text, no duplicate pairs (i.e., if you include 1,3 you cannot include 3,1).
- Exact header row: SourceColumnID1,SourceColumnID2,SimilarityScore,Reason
- SimilarityScore must be a float with exactly two decimal places.
- Reason must be a short English phrase (max 80 characters) explaining the similarity.
- Sort by SimilarityScore DESC.

Example rows:
15,25,0.82,Unique participant/customer identifiers
1,3,0.91,Both represent bet/amount values in transactional events

Begin directly with the CSV header row.

IMPORTANT ADDITIONAL GUIDANCE (apply these rules on top of the original TASK):
- Pay EXTREME attention to natural/business keys and linking identifiers. Examples that MUST be included with high scores (>= 0.85):
  • 62 (VisitID) with 70 (ParentVisitNatKey) — both represent the parent hospital visit/encounter
  • 62 (VisitID) with 74, 79, 63, 76 (any VisitID or ParentVisitNatKey in lab/MRI workflows)
  • 64/73/77 (all MRN columns across EHR/lab/MRI)
  • 45/58 (PatientSex / gender columns)
  • 65/57 (Patient age columns)
- Any column whose description contains “visit”, “encounter”, “parent visit”, “MRN”, “patient identifier”, “natural key”, or “business key” should be considered highly similar to others with the same role, even if the exact column name differs.
- Lower the bar: include ANY pair that could plausibly be joined or used for correlation across workflows (score >= 0.70).

Do NOT be overly conservative — the goal is to surface every possible useful mapping for Time Molecules / event correlation.'


